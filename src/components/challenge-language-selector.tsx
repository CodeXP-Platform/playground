import type { Language } from "@/services/solutions/types";

export default function ChallengeLanguageSelector({
    challengeId,
    languages,
}: {
    challengeId: string;
    languages: Language[];
}) {}
