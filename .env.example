# ─────────────────────────────────────────────
# API Gateway
# Por defecto usa "/api/v1" (ruta relativa, requiere el proxy de Vite en dev).
# Defínela para apuntar a otro host (ej. un tunnel o el gateway desplegado).
# ─────────────────────────────────────────────
VITE_API_GATEWAY_URL=/api/v1
